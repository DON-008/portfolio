TODO: Don to add the real CV PDF here as `Don-Davis-CV.pdf`.

The Hero "Download CV" button links to `/cv/Don-Davis-CV.pdf`
(see `profile.cvPath` in `src/content/data.ts`) — it will 404 until
this file exists. Never a placeholder PDF; this is Don's own document.
