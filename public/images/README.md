TODO: Don to add real project screenshots here (e.g. `esmp.png`,
`pulseticker.png`), then set `Project.screenshot` in
`src/content/data.ts` to the matching `/images/<file>` path.

Cards and case-study pages already render the image conditionally —
no component changes needed once a screenshot is added.
