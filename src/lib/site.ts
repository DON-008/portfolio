// TODO: replace with the real production URL once Step 12 deploys to
// Vercel (or a custom domain). Used for metadataBase, sitemap.xml,
// robots.txt, and absolute OG image URLs. example.com is an RFC 2606
// reserved placeholder domain, never a real site, so it can't be
// mistaken for a live URL if this update gets missed.
export const SITE_URL = "https://example.com";
